import { Pagination } from "react-bootstrap";

const Paginacion = () => {
  let active = 2;
  let items = [];

  
  return (
    <div>
    <Pagination>{items}</Pagination>
    </div>
  );
};
export default Paginacion;
